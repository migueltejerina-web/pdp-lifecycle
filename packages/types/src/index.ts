/**
 * @vistral/types — shared contracts used across apps/* and packages/*.
 *
 * Keep this package tiny. Anything domain-specific (properties, units, contracts)
 * belongs inside the owning app, not here.
 */

export * from './app-slugs'
export * from './roles'
